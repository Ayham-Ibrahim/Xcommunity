<?php

namespace Database\Factories;

use App\Models\Section;
use Illuminate\Database\Eloquent\Factories\Factory;

/**
 * @extends \Illuminate\Database\Eloquent\Factories\Factory<\App\Models\Advertismaent>
 */
class AdvertismaentFactory extends Factory
{
    /**
     * Define the model's default state.
     *
     * @return array<string, mixed>
     */
    public function definition(): array
    {
        return [
            // 'section_id'          => factory(Section::class),
            // 'title'               => fake()->sentence(),
            // 'discription'         => fake()->sentence(),
            // 'image'               => fake()->sentence(),
            // 'trainning_topics'    => fake()->sentence(),
            // 'details'             => fake()->sentence(),
            // 'cost'                => fake()->sentence(),
            // 'tarinning_outcomes'  => fake()->sentence(),
            // 'reservation'         => fake()->sentence(),
        ];
    }
}
